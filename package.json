{
   "name": "@ctrlaltvers/alpine-scripts",
   "description": "Scripts for the package.",
   "version": "1.0.0",
   "scripts": {
      "build": "node ./scripts/build.js",
      "watch": "npm run build -- --watch",
      "release": "node ./scripts/release.js"
   },
   "devDependencies": {
      "@types/alpinejs": "^3.13.11",
      "esbuild": "~0.16.17"
   }
}
